{
  "name": "todoList",
  "version": "1.0.0",
  "description": "aula de javascript(ajax)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --static ./"
  },
  "keywords": [],
  "author": "David Gonzaga gonzagadaviddev@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.16.3"
  }
}
